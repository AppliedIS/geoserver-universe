{
    "id": "/{{ sigma-ui.name }}",
    "cpus": {{ sigma-ui.cpus }},
    "mem": {{ sigma-ui.mem }},
    "instances": 1,
    "container": {
        "type": "DOCKER",
        "docker": {
            "image": "appliedis/sigma-ui",
            "forcePullImage": true,
            "network": "BRIDGE",
            "portMappings": [
                {
                    "containerPort": 3000,
                    "hostPort": 0
                }
            ]
        }
    },
    "constraints": [["hostname", "UNIQUE"]],
    "healthChecks": [{
        "protocol": "HTTP",
        "path": "/health/",
        "portIndex": 0,
        "gracePeriodSeconds": 300,
        "intervalSeconds": 30,
        "timeoutSeconds": 20,
        "maxConsecutiveFailures": 3,
        "ignoreHttp1xx": false
    }],
    "labels": {
        "DCOS_PACKAGE_FRAMEWORK_NAME": "{{ sigma-ui.name }}",
        "HAPROXY_0_VHOST": "{{ sigma-ui.name }}"
    },
    "env": {
        "FRAMEWORK_NAME": "{{ sigma-ui.name }}"
    },
    {{ #sigma-ui.docker-credentials }},
    "uris": [
        "{{ sigma-ui.docker-credentials }}"
    ]
    {{ /sigma-ui.docker-credentials }}
}
